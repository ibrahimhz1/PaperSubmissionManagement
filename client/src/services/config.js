// Centralized client config for API base URL
// On Netlify, set VITE_API_URL in Site settings > Build & deploy > Environment
export const API_BASE = import.meta.env.VITE_API_URL; // e.g., https://your-render-service.onrender.com/api
